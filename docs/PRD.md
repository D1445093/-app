# PRD - 台中大眾運輸交通整合 APP

## 1. 專案概述

### 1.1 背景與動機
大眾運輸的時刻表往往無法反映真實路況或延誤情形。民眾在等待公車或捷運時，常因不確定車輛何時抵達而感到焦慮，或浪費過多時間在站牌等候。本專案旨在整合台中地區的交通數據，提供即時、準確的到站預測，讓使用者能更明確地規劃行程。

### 1.2 目標用戶
台中市民，或來台中需要搭乘大眾運輸的民眾。他們需求是要有準確的到站時間，不只是單單只有時刻表而已，用以節省不必要的等候時間。

### 1.3 核心價值主張
提供準確的即時到站時間，消除「等不到車」的焦慮感，節省不必要的等候時間，透過這款 app 可以讓使用者不必為了等不到公車而焦慮，對於行程的規劃也能夠更加的明確。

---

## 2. 功能需求

以下列出本系統的主要功能與對應的使用者故事：

### 2.1 即時交通數據整合模組（核心）
- **作為** 一名通勤族，**我希望** 能在同一個介面看到公車與捷運的即時到站位置與班次時間，**以便** 精準掌握出門與轉乘的時機。
- *技術細節*：串接 TDX API 獲取台中公車與捷運動態數據，實作後端 Flask 路由與前端自動重新整理機制。

### 2.2 一站式平台查詢
- **作為** 一個外地旅客，**我希望** 能在一個平台上查詢到公車、捷運等多種大眾運輸工具的資訊，**以便** 不用在不同 APP 之間切換。

### 2.3 前端自動重新整理與即時更新
- **作為** 一名正在查看動態資訊的使用者，**我希望** 頁面上的到站時間能自動定時更新（如每 30 秒更新一次），**以便** 我不需要手動刷新頁面就能獲得最新數據。

### 2.4 多運具即時動態整合看板
- **作為** 一名準備轉乘的乘客，**我希望** 在地圖上點擊特定轉乘站點時，能同時看到「公車進站倒數」與「捷運發車時刻」的清單，**以便** 我能快速做決定。

### 2.5 常用路線與轉乘點收藏
- **作為** 一名日常通勤的大學生，**我希望** 能一鍵收藏我每天上下學的路線與站點，**以便** 我下次打開 APP 時能直接查看，減少重複搜尋的時間。

### 2.6 站點擁擠度顯示
- **作為** 一名不喜歡擁擠的乘客，**我希望** 能在 APP 上看到公車或捷運的即時擁擠程度（如：紅-擁擠、綠-通暢），**以便** 我評估是否要等下一班車。

---

## 3. 非功能需求

### 3.1 技術限制
- **前端**：HTML / CSS / JavaScript（純前端，無框架，遵循響應式網頁設計）。
- **後端**：Python + Flask。
- **資料庫**：SQLite，用於儲存使用者的「常用路線」等偏好設定。
- **外部資源**：TDX API（交通運輸資料流通服務平臺）、Leaflet.js（輕量化互動地圖）。

### 3.2 效能與安全考量
- **數據延遲容錯**：需實作快取機制與本地暫存，當 TDX API 失效或延遲時，能顯示最後一筆有效資訊並標記「離線」狀態。
- **重新整理機制**：在地圖上明確標示資料更新時間，並允許前端介面手動/自動觸發最新位置請求，確保及時定位的精確性。

---

## 4. MVP 範圍

### Must Have（核心必須功能）
- 即時交通數據整合模組（串接 TDX API，顯示公車/捷運動態）。
- 實作後端 Flask 路由與前端自動重新整理機制，以即時呈現車輛到站位置與班次時間。
- 互動式地圖呈現（使用 Leaflet.js 標記站牌與車輛位置）。

### Should Have（重要但不影響核心運作）
- 多運具轉乘路徑演算（捷運+公車最佳化組合）。
- 常用路線與轉乘點「一鍵收藏」功能。

### Nice to Have（加分項目）
- 試算時間與價格功能（即時計算總花費）。
- 站點狀態與擁擠度顯示（視覺化指標）。

---

## 5. 專案成員與分工

| 姓名 | 負責功能模組 | 對應功能編號 | 說明 |
| --- | --- | --- | --- |
| 林紫誼 | 即時交通數據整合模組 | F-02 | 串接 TDX API 獲取動態數據，實作後端路由與前端重整機制 |
| 鍾晴伃 | 一站式平台 | F-01 | 整合大眾運輸資訊，設計前端查詢頁面與後端路由 |
| 鄭鈺穎 | 多運具轉乘路徑演算模組 | F-03 | 開發跨運具路徑規劃演算法，預估 ETA |
| 盧泇羽 | 試算時間與價格功能 | F-04 | 串接費率資料庫，計算預計時間與總花費 |
| 鄭庭竹 | 個人化收藏管理 | F-05 | 開發「一鍵收藏」功能，透過 LocalStorage 或 SQLite 儲存 |
| 楊薰嬪 | 互動地圖與資訊整合看板 | F-06 | 地圖標記點與側邊欄聯動，併列呈現多運具即時數據 |
| 陳姿妘 | 站點狀態與擁擠度顯示 | F-07 | 提取站點即時人流資訊，前端視覺化擁擠度指標 |
